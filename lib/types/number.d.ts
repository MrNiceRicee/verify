import Base from '../base';
import { generalOptions, internalOptions } from '../typedefs';
declare const isNumber: (from: Base, options: generalOptions, internal: internalOptions) => Base;
export default isNumber;
