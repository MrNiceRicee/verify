import Base from '../base';
import { generalOptions, internalOptions } from '../typedefs';
declare const isInt: (from: Base, options: generalOptions, internal: internalOptions) => Base;
export default isInt;
