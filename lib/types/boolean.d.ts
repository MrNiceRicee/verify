import Base from '../base';
import { generalOptions, internalOptions } from '../typedefs';
declare const isBoolean: (from: Base, options: generalOptions, internal: internalOptions) => Base;
export default isBoolean;
