import Base from '../base';
import { generalOptions, internalOptions } from '../typedefs';
declare const isString: (from: Base, options: generalOptions, internal: internalOptions) => Base;
export default isString;
