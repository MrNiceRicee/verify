import Base from '../base';
import { generalOptions, internalOptions } from '../typedefs';
declare const isArray: (from: Base, options: generalOptions, internal: internalOptions) => Base;
export default isArray;
