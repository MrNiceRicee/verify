import Base from '../base';
import { compareOptions, internalOptions } from '../typedefs';
declare const isLT: (compare: any, from: Base, options: compareOptions, internal: internalOptions) => Base;
export default isLT;
