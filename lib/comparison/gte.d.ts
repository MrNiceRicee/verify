import Base from '../base';
import { compareOptions, internalOptions } from '../typedefs';
declare const isGTE: (compare: any, from: Base, options: compareOptions, internal: internalOptions) => Base;
export default isGTE;
