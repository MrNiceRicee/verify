import Base from '../base';
import { compareOptions, internalOptions } from '../typedefs';
declare const isLTE: (compare: any, from: Base, options: compareOptions, internal: internalOptions) => Base;
export default isLTE;
