import Base from '../base';
import { compareOptions, internalOptions } from '../typedefs';
declare const isGT: (compare: any, from: Base, options: compareOptions, internal: internalOptions) => Base;
export default isGT;
